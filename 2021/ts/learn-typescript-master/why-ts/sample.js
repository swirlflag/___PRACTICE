// @ts-check

/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
const add = (a,b) => {
    return a + b
}

/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자1
 */
const add2 = (a,b) => {
    return (a + b) * 2;
}

add(10,10);
add2(10,20);